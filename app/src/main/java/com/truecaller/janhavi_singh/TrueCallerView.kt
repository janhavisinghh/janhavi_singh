package com.truecaller.janhavi_singh

interface TrueCallerView {
    fun displayTenthCharRequest(it: String)
    fun displayEveryTenthCharRequest(it: String)
    fun displayWordCounterRequest(it: String)
}